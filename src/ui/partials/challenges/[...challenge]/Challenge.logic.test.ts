import { ChallengePageLogic } from './Challenge.logic';

describe('ChallengePageLogic', () => {

    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test ChallengePageLogic', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn(ChallengePageLogic, ChallengePageLogic.test.name as keyof typeof ChallengePageLogic);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mockedChallengePageLogic = jest.mocked(ChallengePageLogic);
        // mockedChallengePageLogic.test.mockReturnValue(456);

    });
});
