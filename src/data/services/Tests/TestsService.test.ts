import { TestsService } from './TestsService';

describe('TestsService', () => {
    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test TestsService', () => {
        // expect(true).toBe(true);
        // const spy = jest
        //     .spyOn(TestsService, TestsService.test.name as keyof typeof TestsService);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);
        // const mockedTestsService = jest.mocked(TestsService);
        // mockedTestsService.test.mockReturnValue(456);
    });
});
