import { {{name}}PageLogic } from './{{name}}.logic';

describe('{{name}}PageLogic', () => {

    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test {{name}}PageLogic', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn({{name}}PageLogic, {{name}}PageLogic.test.name as keyof typeof {{name}}PageLogic);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mocked{{name}}PageLogic = jest.mocked({{name}}PageLogic);
        // mocked{{name}}PageLogic.test.mockReturnValue(456);

    });
});
